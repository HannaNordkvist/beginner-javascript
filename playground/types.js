/* eslint-disable */

const name = 'hanna';
const middle = "elisabet";
const last = `nordkvist`;
const y = 'years old';

const mening = "en mening";

const sentence = `det här är en mening
som
kan delas upp på
flera
rader`;

const hello = `hello my name is ${name}. 
Nice to meet you.
Jag är ${3 + 34} ${y}`;

const html = `
<div>
</div>`;

const smarties = 20;
const kids = 3;
const eachKidGets = Math.floor(smarties/kids);
const parentGet = smarties % kids;
console.log(`each kids get ${eachKidGets} and I get ${parentGet}`);

// SKRIV I CONSOLE: window.location = `https://${0.1 + 0.2}.com`;

const person = {
    first: 'hanna',
    last: 'nordkvist',
    age: 45
};

let isDrawing = false;
const agetwo = 20;
const ofAge = agetwo > 19;
console.log(ofAge);











